var Reducer = function (state, action) {
    if (typeof state === 'undefined') {
        return {
            msg: "45"
        }
    }
    switch (action.type) {

        default:
            return action
    }
}
export default Reducer